# Enter your code here. Read input from STDIN. Print output to STDOUT
from itertools import permutations
S , k=input().split()
x=(list(permutations(sorted(S),int(k))))
for i in x:
    print(''.join(i))


# from itertools import permutations
# s,n = input().split()
# print(*[''.join(i) for i in permutations(sorted(s),int(n))],sep='\n')
